
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div> 
        @{ 
            int m = 10;
            string s = "<h1>demo</h1>";
            string[] data = { "it1", "it2", "it3" };
        }
        <ul>
            <li>不用轉字串 @m</li>
            <li>自動做HtmlEncode @s</li>
            <li>不做HtmlEncode @Html.Raw(s)</li>  <!--Html.Raw 表示直接顯示原始內容 不另外做處理-->
        </ul>
        @*for  Razor使用這個方式註解 不會顯示在原始檔編碼上 *@
        <ul>
            @for(int i = 0; i < data.Length; i++)
            {
                <li>@data[i]</li>
            }
        </ul>
        @*foreach*@
        <ul>
            @foreach(var it in data)
            {
                <li>@it</li>
            }
        </ul>
        @*If*@
        @if (true)
        {
            @m
            <text>@m</text>
            @:@m
}
    </div>
</body>
</html>
